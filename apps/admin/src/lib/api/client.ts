/** Re-export shim: the admin's client moved into `@skinny/api-client`. */
export { ApiError, buildQuery } from '@skinny/api-client';
export type { ApiClient as AdminApi, TokenProvider } from '@skinny/api-client';
