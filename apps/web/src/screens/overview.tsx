import { PlaceholderScreen } from './placeholder';

/** Tổng quan — the dashboard (Task 7). */
export function Component() {
  return <PlaceholderScreen titleKey="overview.title" bodyKey="overview.empty" />;
}

Component.displayName = 'OverviewScreen';
