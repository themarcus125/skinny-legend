import { PlaceholderScreen } from './placeholder';

/** MemberDetail — /leaderboard/:userId (Task 9). */
export function Component() {
  return <PlaceholderScreen titleKey="leaderboard.title" bodyKey="leaderboard.memberEmpty" />;
}

Component.displayName = 'MemberScreen';
