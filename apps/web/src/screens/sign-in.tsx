import { PlaceholderScreen } from './placeholder';

/** SignInView — Google sign-in and the gate states (Task 6). */
export function Component() {
  return <PlaceholderScreen titleKey="auth.title" bodyKey="auth.tagline" />;
}

Component.displayName = 'SignInScreen';
