import { PlaceholderScreen } from './placeholder';

/** Feed — the group log grouped by day (Task 10). */
export function Component() {
  return <PlaceholderScreen titleKey="feed.title" bodyKey="feed.empty" />;
}

Component.displayName = 'FeedScreen';
