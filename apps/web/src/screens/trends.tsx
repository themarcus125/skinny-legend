import { PlaceholderScreen } from './placeholder';

/** Xu hướng — weekly bars and the active-days heatmap (Task 11). */
export function Component() {
  return <PlaceholderScreen titleKey="trends.title" bodyKey="trends.empty" />;
}

Component.displayName = 'TrendsScreen';
