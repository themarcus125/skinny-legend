import { PlaceholderScreen } from './placeholder';

/** Ghi nhận — capture, upload, verdict sheet (Task 8). */
export function Component() {
  return <PlaceholderScreen titleKey="track.title" bodyKey="track.prompt" />;
}

Component.displayName = 'TrackScreen';
