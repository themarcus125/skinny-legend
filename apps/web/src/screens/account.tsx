import { PlaceholderScreen } from './placeholder';

/** Account — profile, language, theme, reminders (Task 13). */
export function Component() {
  return <PlaceholderScreen titleKey="account.title" bodyKey="account.historyEmpty" />;
}

Component.displayName = 'AccountScreen';
