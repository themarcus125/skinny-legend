/**
 * Account — profile, language, appearance and my history (Task 12). The screen itself lives in
 * `src/features/account/account.tsx`; this module is only the route's lazy entry point, so the
 * router keeps one convention (`Component`) and the feature keeps its own folder.
 */
export { Component } from '@/features/account/account';
